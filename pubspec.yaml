name: tram_sac
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  # Supabase client
  supabase_flutter: ^2.5.0
  connectivity_plus: ^6.0.3
  intl: ^0.18.1
  # Thư viện bản đồ OpenStreetMap
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

  # Các gói tiện ích khác
  location: ^5.0.3
  cupertino_icons: ^1.0.8
  screen_protector: ^1.4.5
  font_awesome_flutter: ^10.7.0
  http: ^1.2.0
  qr_flutter: ^4.1.0
  mailer: ^6.0.0
  image_picker: ^1.2.1
  google_sign_in: ^7.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

